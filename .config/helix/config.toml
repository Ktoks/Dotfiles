theme = "gruvbox_material_dark_hard"

[editor]
line-number = "relative"
mouse = true
scrolloff = 6
bufferline = "multiple"
shell = ["bash", "-c"]
auto-completion = true
completion-trigger-len = 1
end-of-line-diagnostics = "hint"
color-modes = true
undercurl = true
# Increases the time Helix waits before triggering hover popups or LSP requests.
# This prevents spamming the network drive if you rapidly scroll through a file.
idle-timeout = 600

[editor.inline-diagnostics]
cursor-line = "hint"
other-lines = "hint"

[editor.cursor-shape]
insert = "bar"

[editor.file-picker]
# max-depth = 4
# Prevents Helix's internal file picker from trying to read deeply nested
# ignored folders (like node_modules) over the network.
hidden = false
git-ignore = true
git-global = true

[editor.gutters]
layout = ["diff", "diagnostics", "line-numbers", "spacer", "spacer"]

[editor.gutters.line-numbers]
min-width = 3

[editor.indent-guides]
render = true
character = "|"
skip-levels = 1

[editor.lsp]
enable = true
# Extends the timeout for format-on-save. If Rclone is busy writing a file,
# we want to give the LSP more time to format it before giving up.
auto-signature-help = false # Prevents constant network queries on every keystroke
display-messages = true     # Lets you see if the LSP is indexing in the background

[editor.search]
smart-case = true
wrap-around = true

[editor.statusline]
left = ["mode", "spinner", "file-name", "position-percentage"]
center = ["selections"]
right = ["position", "spacer", "diagnostics", "register", "file-encoding"]

[keys.normal.g]
l = "code_action"

[keys.normal."space"]
o = ":config-open"
r = ":config-reload"
c = ":buffer-close"
C-h = "swap_view_left"
C-l = "swap_view_right"
C-j = "swap_view_down"
C-k = "swap_view_up"

[keys.normal]
# C-y = [
#   ':sh rm -f /tmp/unique-file',
#   ':insert-output yazi %{buffer_name} --chooser-file=/tmp/unique-file',
#   ':insert-output echo "\x1b[?1049h\x1b[?2004h" > /dev/tty',
#   ':open %sh{cat /tmp/unique-file}',
#   ':redraw',
# ]
    
H = "goto_previous_buffer"
L = "goto_next_buffer"

C-h = "select_prev_sibling"
C-j = "shrink_selection"
C-k = "expand_selection"
C-l = "select_next_sibling"

[keys.select]
H = "goto_previous_buffer"
L = "goto_next_buffer"


[keys.insert.j]
k = ["normal_mode"]

